flong check: @GOLDEN@/decl/a-bind-of-a-device.zon: flong.box drives containers.box, which binds /dev/snd. A plain bind is nodev, so the device would mount and then refuse every open. List it in allowedDevices instead, and drop the bind.
